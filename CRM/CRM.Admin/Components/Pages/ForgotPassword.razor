@layout CenteredCardLayout
@page "/ForgotPassword"
@using CRM.Admin.Components.Layout
@inject NavigationManager NavigationManager

<EditForm Model="@user" OnValidSubmit="ValidateUser">
    <DataAnnotationsValidator/>
    <MudCardContent Class="pb-0">
        <MudTextField Label="Email" Id="Email" Required="true" Variant="Variant.Outlined" @bind-Value="user.Email" For="@(() => user.Email)"
                      Immediate="true" InputType="InputType.Email"/>
        <br/>
    </MudCardContent>
    <br/>
    <div>
        <MudCardActions Class="d-flex">
            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Size="Size.Large" EndIcon="@Icons.Material.Filled.Login" Style="width: 100%; height: 56px">Ok</MudButton>
        </MudCardActions>
    </div>
</EditForm>